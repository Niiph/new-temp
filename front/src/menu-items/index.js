// project import
import pages from './pages';
import devices from './devices';
import utilities from './utilities';
import support from './support';

// ==============================|| MENU ITEMS ||============================== //

const menuItems = {
    items: [devices, pages, utilities, support]
};

export default menuItems;
