// project import
import pages from './pages';
import devices from './devices';

// ==============================|| MENU ITEMS ||============================== //

const menuItems = {
    items: [devices, pages]
};

export default menuItems;
